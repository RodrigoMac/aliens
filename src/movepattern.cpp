#include "../include/movepattern.h"

void movepattern(int const& pattern)
{
    float xSpd, ySpd;
    int timer;
    
    switch(pattern)
    {
        case STRAIGHT:
            xSpd = 0;
            ySpd = 0.25;
        
    }
}
